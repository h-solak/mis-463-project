import { Grid } from "@mui/material";
import React from "react";

const Option = ({ children, onClick, isActive }) => {
  return <div>FilterForm</div>;
};

const FilterForm = () => {
  return (
    <Grid item md={4}>
      FilterForm
    </Grid>
  );
};

export default FilterForm;
